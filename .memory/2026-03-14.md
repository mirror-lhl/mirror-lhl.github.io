# 2026-03-14 记忆日志

## freelancer-crm.html 新增 Enterprise 套餐 ¥1,000,000/year
- 套餐体系由3档扩展为4档：Starter(Free) / Professional(¥10,000) / Team(¥100,000) / Enterprise(¥1,000,000)
- 网格从 md:grid-cols-3 改为 md:grid-cols-2 xl:grid-cols-4
- payment.html 的 freelancer-crm plans 同步加入 enterprise: { amount: '1000000.00' }

## freelancer-crm.html 悬浮栏 stickyBuyBar bug 修复
- 问题1：切换套餐时悬浮栏金额不更新 → 根本原因：`<script>` 在悬浮栏 HTML 之前执行，`getElementById('stickyPlanLabel')` 和 `getElementById('stickyPayBtn')` 返回 null，事件绑定失败
- 问题2：点击 Pay Now 按钮无反应 → 同上，stickyBtn 为 null，addEventListener 绑定到 null 报错
- 修复：将悬浮栏 HTML 移到 `<script>` 之前，确保 DOM 元素存在再取引用
- 优化：悬浮栏左侧显示「套餐名 · ¥金额」，右侧按钮显示「💳 Pay Now · ¥金额」更直观


## 货币符号统一修改（$ → ¥）（第二轮）
- index.html 第57/88/119/150/181行的 Monthly/Annual Revenue Target 金额范围也全部改为 ¥
- grep 验证 index.html 内无任何残留 $

## 货币符号统一修改（$ → ¥）（第一轮）
- 用户要求：所有项目文件金额符号统一为人民币 ¥，不要混用美元 $
- 用户要求一个一个文件修改，不要批量修改
- 已完成 index.html：7处 $ 替换为 ¥（Revenue Target 区、Validation/Scaling/Platform/Monopoly Phase 各目标值）
- 已完成 join-us.html：
  - 佣金阶梯表格（5行共10处 $ → ¥）
  - 计算器结果展示初始值（resultSales/resultEarning/resultMonthly 的 $0.00 → ¥0.00）
  - 输入框标签 Sales Performance ($) → (¥)
  - formatCurrency() JS 函数里 '$' 拼接全部改为 '¥'
- 其他文件（script-generator/geo-optimizer/freelancer-crm/ai-creator/local-life/enterprise-ai）经 grep 确认无 $数字，无需修改
